# Dec 项目变量定义
# 资产模板中的 {{VAR_NAME}} 会在 dec pull 时替换
# 优先级：assets.<type>.<name>.vars > vars > 机器级变量 (~/.dec/local/vars.yaml)

vars:
  # API_BASE_URL: "https://api.example.com"
  # API_TOKEN: "<TOKEN>"
  TASK_DOCS_DIR: "docs/tasks"
  VIKUNJA_PROJECT: "vikunja-mcp"

assets:
  skill:
    # my-skill:
    #   vars:
    #     API_TOKEN: "<TOKEN>"
  rule:
    # my-rule:
    #   vars:
    #     API_BASE_URL: "https://api.example.com"
  mcp:
    # my-mcp:
    #   vars:
    #     API_TOKEN: "<TOKEN>"
